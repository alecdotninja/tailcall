[package]
name = "std-integration"
version = "0.0.0"
edition.workspace = true
publish = false

[dependencies]
tailcall = { path = "../tailcall" }

[dev-dependencies]
backtrace = "~0.3"
criterion = { version = "~0.5", default-features = false }

[[bench]]
name = "bench"
harness = false
